kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: apulis-mysql-data-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.mysql.dataClaimCapacity }}
