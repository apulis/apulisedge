apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mysql.dbAppName }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.mysql.dbAppName }}
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP